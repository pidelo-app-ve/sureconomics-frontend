## Vite environment variables
## Copy to `.env` and adjust as needed.

# Backend base URL (Flask API)
VITE_API_URL=http://127.0.0.1:5000

# Router basename (keep "/" for most setups)
BASENAME=/

BASENAME=/